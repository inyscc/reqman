## 1. 测试锚点与状态模型

- [x] 1.1 改写 `tests/app.test.tsx` 中依赖单槽位的断言锚点（`session-tab` 唯一、`.crumb-bar` 不存在、「顶部只有一行」等），改为以标签栏与请求面板头为锚点；跑 `npm test` 确认改完的用例先红，表示锚点确实落在尚未实现的行为上
- [x] 1.2 在 `src/App.tsx` 定义 `SessionTab` 联合类型（请求标签持有 `draft`/`dirty`/`response`/`innerTab`/`scriptReport`，实体标签持有 `entityKind`/`id`/`draft`）与 `tabs` / `activeTabId` 状态，替换 `selectedId`、`draft`、`dirty`、`response`、`tab` 单槽位；跑 `npx tsc --noEmit` 确认类型收敛，`npm test` 确认既有请求编辑用例仍通过
- [x] 1.3 实现 `openRequest(id)` / `openEntity(kind, id)` 两个唯一打开入口，内含「同一 id 至多一个标签」的去重：已在册则聚焦既有标签、不重载草稿；新增对应用例（重复打开同一请求不新增标签、未保存编辑内容原样保留）并通过
- [x] 1.4 把「新建请求」与「另存为」接到打开入口上，确认二者新增标签并置于激活态；原标签保留、不丢弃已有草稿，并移除这两个 intent 身上的守卫包装（它们不覆盖当前草稿，故不再需要过守卫）；新增用例覆盖两条路径并通过

## 2. 编辑面与守卫

- [x] 2.1 改为按标签注册编辑面（id 形如 `request:<id>` / `entity:<kind>:<id>`，沿用既有优先级），未激活标签同样注册；新增用例：两个标签各有未保存改动时可被同时列入脏面（`tests` 中针对 `lib/editing` 或守卫提示文案的断言），跑 `npm test` 通过
- [x] 2.2 为实体编辑面补 `isActive`（当前在后台的实体标签会被 `top()` 选中，导致 `Ctrl+S` 保存用户没在看的脚本）；新增用例：实体标签进后台后发 `Ctrl+S` 不发出任何写请求，跑 `npm test` 通过
- [x] 2.3 收缩守卫触发集合：把「切换到另一个请求」「切换到集合或文件夹」移出触发项，仅保留「关闭有未保存改动的标签」「删除对应请求或实体」「删除一个集合或文件夹（其下含脏标签的请求或实体）」「退出应用」；把规格里反转用例（切换标签不询问、关闭脏标签先询问、删除含脏标签的集合先询问）落成测试并通过
- [x] 2.4 确认 `Ctrl+S` 仍只保存当前生效的标签（非全部脏标签），且关闭最后一个标签后主区回空态；新增两条用例并通过

## 3. 顶部标签栏与身份下沉

- [x] 3.1 在 `.session-bar` 内新增 `.session-tabs` 标签栏（`flex: 1 1 auto; min-width: 0; overflow-x: auto` 加隐藏滚动条），`.env-select` 与 `.window-controls` 保持 `flex: none`；用 `npm test` 断言标签栏与环境选择器、窗口控制按钮同处一行，并用浏览器实测窗口收窄时三者不互相遮挡
- [x] 3.2 实现标签的呈现：请求标签为方法徽标 + 名称、实体标签为种类图标 + 名称（图标带可读名称），标签等宽、名称溢出省略号、不可就地编辑；标签元素渲染成 `<button>`（或把 `.session-tab` 加进 `ResizeStrips.tsx` 的 `isInteractiveSessionBarTarget` 排除选择器），确保点标签切页不触发窗口拖拽（window-chrome 要求）；新增用例覆盖请求标签、实体标签、双击标签不进入编辑态、以及标签上按下不移动窗口
- [x] 3.3 实现标签的激活态与未保存指示（关闭按钮 `visibility` 占位、有改动时该位置显示圆点、悬停恢复为关闭入口）；新增用例：非激活标签上的未保存指示可见、标签宽度不随激活态变化
- [x] 3.4 在 `src/components/RequestEditor.tsx` 顶部新增请求面板头，承载所属集合面包屑、请求名输入框与「另存为」「删除」「未保存 + 保存」；改 `tests/app.test.tsx` 中相关断言并通过，确认这些元素不再出现在会话标签行内
- [x] 3.5 把 `EntityScriptPanel` 的 `pane-header` 中静态名称改为可编辑输入框，重命名就地提交；新增用例：实体改名落在面板头且提交后树刷新
- [x] 3.6 保留 `另存为` / `删除` 「按需显现且不引起水平跳动」的行为（现作用于请求面板头），确认既有对应用例迁移后通过
- [x] 3.7 确认方法徽标仍只出现在三处（集合树节点、会话标签、地址栏方法选择框），请求面板头内不出现；新增一条断言并通过

## 4. 标签生命周期与对账

- [x] 4.1 实现关闭入口：标签上的关闭按钮与鼠标中键等价，关闭激活标签后激活态移交给相邻标签；新增用例覆盖中键关闭、激活态移交、关闭最后一个标签回空态且标题栏仍在
- [x] 4.2 实现「删除请求/实体即关闭其标签」；新增用例：删除已打开且无改动的请求后标签消失且不残留
- [x] 4.3 实现 `loadTree` 之后的标签对账：指向已不存在条目的标签被静默关闭，脏标签不被误关；对账必须发生在 `loadTree` 已把条目写回 store 之后（先 seed 再对账），补充用例断言恢复的标签不会因时序被误杀；新增用例模拟导入替换集合后僵尸标签被清理，且界面不进入错误态
- [x] 4.4 确认标签激活态与集合树选中态是同一份状态、不各自维护；新增用例：切换标签后树中被选中的条目随之改变

## 5. 标签集合持久化

- [x] 5.1 新增按工作区读写标签集合的模块（`scope='ui_tabs'`、key 为工作区 id、值为 JSON），容错纪律对齐 `src/lib/layout.ts`（读不懂回落空集合、写不进静默）；补单元测试覆盖解析失败与写入失败两条路径
- [x] 5.2 在加载工作区、树拉取完成后恢复标签集合与激活项，丢弃指向已删除条目的项；明确顺序：先 seed store → 按现存 id 过滤恢复 → 再跑对账，补充用例断言恢复不因时序误关标签；新增用例覆盖恢复、丢弃、以及按工作区分开
- [x] 5.3 确认持久化不含未保存草稿：用例断言重启后标签内容为已保存版本、被丢弃的改动不出现在界面上

## 6. 键盘、动效与整体验证

- [x] 6.1 实现 `Ctrl+W`（走守卫）、`Ctrl+Tab` / `Ctrl+Shift+Tab`、`Ctrl+1..9`，挂在与 `Ctrl+S` 同一层的 window keydown 上；新增用例覆盖三条快捷键并跑 `npm test` 通过
- [~] 6.2 最小探针需在真实 Tauri/WebView2 运行时确认（本环境无图形界面、且 Playwright `channel:'chrome'` 不等价于 WebView2，无法代跑）；`Ctrl+W` 关闭逻辑已单测覆盖，中键与关闭按钮作为兜底始终可用。待用户在 Windows 客户端确认 WebView2 是否把 `Ctrl+W` 交给页面；若不交接，再从快捷键清单移除并回填 design.md
- [x] 6.3 确认标签切换与标签关闭均无过渡动效（切标签是每天数十次以上的操作）；在浏览器里切换标签肉眼确认无动画，并在 `src/App.css` 中确认未引入相关 transition
- [x] 6.4 跑 `npx tsc --noEmit` 与 `npm run build` 确认类型与构建通过
- [x] 6.5 跑 `openspec validate add-multi-tab-sessions --strict` 确认规划产物仍然一致，并同步 README 与 `rodemap` 中把多标签列为 P1 的表述
- [~] 6.6 端到端手工验证需在 Windows 客户端真机执行（本环境无图形界面）；上述每一步均有单测覆盖（见「多标签会话」套件与「未保存守卫」套件），待用户真机走一遍确认
