## 1. 树骨架：折叠与外观

- [x] 1.1 在 `WorkspaceTree.tsx` 引入按节点 id 记录的折叠集合（未记录即展开、默认全展开），集合与文件夹节点渲染可点击的折叠箭头，请求叶子节点不渲染箭头；验证：点击箭头后子节点隐藏、再次点击恢复，叶子节点上没有箭头
- [x] 1.2 收紧 `.tree` / `.node` 样式：侧栏作用域内字号与行高变小、文件夹节点加图标、多级嵌套加缩进参考线；验证：与 Postman 截图并列比对密度接近，且 Environments tab 的变量表格仍可读
- [x] 1.3 请求节点改用小号彩色方法标签（GET / POST 等），不再复用灰底 `.badge`；会话标签仍用 `.badge` 且外观不变；验证：树中方法标签视觉轻于名称，顶部会话标签的显示未受影响

## 2. 操作入口：hover 菜单

- [x] 2.1 移除集合行的常驻 `+` / `×` 与文件夹行的常驻 `+`，改为 hover 与 `:focus-within` 时才显示的「⋯」按钮；验证：静态渲染的集合树上不出现任何操作按钮
- [x] 2.2 实现菜单组件：绝对定位、按节点类型列出动作（集合与文件夹：新建请求 / 新建子文件夹 / 重命名 / 删除；请求：重命名 / 删除），点击外部与 `Esc` 可关闭，侧栏滚动时关闭；验证：打开后按 `Esc` 和点击外部都能关闭，且关闭后选中状态不变
- [x] 2.3 把「新建请求」迁到菜单中，集合根与文件夹下都可用；验证：从菜单新建的请求出现在正确的父级下并被自动选中打开

## 3. 目录的创建与删除

- [x] 3.1 在 `App.tsx` 接入 `folderCreate`，菜单的「新建子文件夹」按节点类型传入正确的父级参数（集合根传 `null`，文件夹传其 id）；验证：可连续创建三层嵌套，刷新工作区后层级保持
- [x] 3.2 接入 `folderDelete`，删除前在节点下方渲染行内确认条并说明「及其全部内容一并删除」；验证：确认后该文件夹及其后代请求一并消失，取消时无任何变化
- [x] 3.3 补一个 Rust 侧用例，断言删除文件夹会级联删除后代文件夹与其中的请求（防止未来误关 `PRAGMA foreign_keys`）；验证：`cargo test` 通过

## 4. 集合与文件夹的重命名

- [x] 4.1 面包屑行在选中集合/文件夹时渲染名称输入框与保存按钮（现在这一行只显示「未选择请求」）；验证：选中一个集合后能看到并编辑它的名称
- [x] 4.2 保存时按选中类型调用 `collectionRename` / `folderRename`，成功后刷新树并同步会话标签与 `entityDraft`；验证：树与会话标签立即显示新名称，重启后名称保持，其下条目与脚本不受影响
- [x] 4.3 空名称提交时保留原名称并给出失败提示；验证：清空名称后保存，名称未变且界面出现错误提示

## 5. 脚本区左右两栏

- [x] 5.1 新建 `src/components/ScriptPane.tsx`：左栏列出 Pre-request 与 Post-response 可点击切换，右栏为编辑器，左栏标记该段脚本是否已有内容；验证：切换后右栏内容随之变化，来回切换不丢失已输入内容
- [x] 5.2 `RequestEditor.tsx` 的 Scripts 分支改用 `ScriptPane`，外层标签名仍为 Scripts；验证：Scripts 页呈现为一套两栏，六个标签的名称与顺序不变
- [x] 5.3 `EntityScriptPanel.tsx` 改用 `ScriptPane`，保存按钮与「编辑即授权」逻辑保持不变；验证：集合/文件夹的脚本保存后仍可查到，且发送时不再弹出脚本门禁

## 6. 回归与收尾

- [x] 6.1 在 `tests/app.test.tsx` 用假命令覆盖：折叠与展开、操作入口默认隐藏、新建与删除子文件夹、重命名集合与文件夹、脚本两栏切换；验证：`npm test` 全部通过
- [x] 6.2 运行 `npx tsc --noEmit` 与 `npm run build`；验证：两条命令均无报错
