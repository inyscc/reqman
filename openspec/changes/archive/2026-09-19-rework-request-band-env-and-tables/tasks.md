## 1. 先钉死两个不确定的现状（design D4 风险项）

- [x] 1.1 确认表头吸顶的现状并留下回归：在真实 Chrome（复用本机 Chrome，不下载浏览器）里滚动一条参数很多的请求表，记录表头是否钉住、下方内容是否从表头透出；验证方式：`npm run dev` 观察并记录结论——结论为「已生效」时在 `tests-browser/` 补一条回归断言，结论为「未生效」时把现象并入 3.1 一并修
- [x] 1.2 确认 `.session-bar` 放开 `overflow` 后标签溢出的行为（标签多到超宽时是否仍由标签栏自身滚动、环境选择器与窗口按钮是否仍完整可见）；验证方式：在 dev 环境打开 10 个以上标签观察该行布局

## 2. 通栏请求带（spec: 请求面板头的身份与操作 / 地址栏与解析预览条 / 主区左右分栏与可调比例）

- [x] 2.1 把 `.main` 改成三行网格（会话标签行 / 请求带 / 分栏区），请求带为 `grid-column: 1 / -1`，`.request-region`、`.split-handle`、`.response-region` 显式落到第三行；验证方式：dev 环境里拖动分隔线，地址栏与解析预览条的宽度不变，`npm test` 中主区结构相关用例全绿
- [x] 2.2 拆分 `RequestEditor`：请求带（请求身份行、地址栏、解析预览条）与列内容（内层标签与正文）分别渲染，`error` 与 `scriptGate` 提示块落在请求带下方的通栏项；验证方式：新增单测断言打开请求时请求身份行与地址栏位于主区顶层容器内且先于提示块渲染
- [x] 2.3 回归分栏的既有语义：比例仍是宽度比、仍按工作区持久化、`SPLIT_SCOPE` 与钳制区间不变；验证方式：`npm test -- tests/layout.test.ts tests/app.test.tsx` 中分栏用例通过
- [x] 2.4 「另存为 / 删除」按需显现的行为在新行宽下仍成立且不引起面包屑水平跳动；验证方式：单测覆盖「指针不在请求带内时二者不可见」「移入后可见且面包屑位置不变」

## 3. 键值表表面与描述列（spec: 表格轻量化与 hover 高亮 / 键值表的列与描述列）

- [x] 3.1 把 `table` 由 `border-collapse: collapse` 改为 `separate; border-spacing: 0`，行分隔落到单元格底边，表头改成不透明底 + 更明显的底线；验证方式：dev 环境滚动表格时表头钉住且下方行不从表头透出，`npm test` 全绿
- [x] 3.2 单元格静默态去掉填充底色与完整边框，仅在 hover / 聚焦时显现边界；掩码文本与徽标保持与可编辑单元格相同的行高与内边距；验证方式：dev 环境目视确认幽灵行与内容行等高（既有约束），`npm test -- tests/request-editor.test.tsx` 通过
- [x] 3.3 变量表沿用同一套表面规则（其值仍以输入框呈现，但静默态无填充无边框）；验证方式：环境编辑器里滚动变量表，表头钉住、行 hover 可见、掩码行与输入行等高
- [x] 3.4 在 Params、Headers、x-www-form-urlencoded 三张表插入**可就地编辑**的描述列，幽灵行同步补一格；验证方式：新增单测断言三张表各有描述输入、可在其中输入并修改值、幽灵行的描述输入存在
- [x] 3.5 把空行清洗拆成两档：保留判定（保存与打开，名称、值、描述三者皆空才算空行）与发出判定（名称与值皆空即不构成请求数据）分离；验证方式：`tests/rows.test.ts` 新增用例——只填描述的行「被保留」「不进 `cleanForSend` 结果」「名称为空但有值的行照旧进入载荷（不静默丢弃）」，两条判定同组完成
- [x] 3.6 参数表按查询串重建时保留「名称为空、描述非空」的行（覆盖打开请求的 `alignUrlAndParams` 与地址栏输入的 `withUrl` 两条路径，并让 `composeUrl` 跳过这类行）；验证方式：`tests/url.test.ts` 新增用例——带描述的空名行在 `withUrl` 之后仍在、反复编辑不复制，且不出现在合成出的 URL 里
- [x] 3.7 回归描述的持久化与导入导出往返；验证方式：单测断言「填写描述并保存后重新打开仍在」，`cargo test` 中 `KeyValue.description` 相关用例通过

## 4. 请求体类型选择行（spec: 请求体类型的选择行）

- [x] 4.1 用一行互斥单选替换 Body 标签页中的原生类型下拉，文案为 `none` / `form-data` / `x-www-form-urlencoded` / `raw` / `binary`；验证方式：单测按可访问名称切换类型，表格随之切换
- [x] 4.2 raw 的语言选择内联到类型行行尾且仅在 raw 时出现；验证方式：单测断言非 raw 时该控件不存在、选中 raw 后出现并可切换语言
- [x] 4.3 保持「切换类型清掉其它类型残留内容」的既有行为；验证方式：单测覆盖一次 raw → form-data 的切换，断言 raw 正文被清空

## 5. 环境列表（spec: Environments tab 的列表观感与搜索）

- [x] 5.1 行高提到约 32px 并解除与集合树同密度的耦合，激活态改为行首勾选标记 + 整行浅底，去掉「使用中」徽标、左侧强调竖条与 Globals 副标题；验证方式：`tests/app.test.tsx` 中原断言「使用中」的用例改为断言勾选标记存在且不存在该徽标文案
- [x] 5.2 工具栏增加按环境名称的搜索输入（纯前端过滤、不写后端、不改激活态）；验证方式：新增单测覆盖「命中过滤」「无命中显示空态且激活不变」「清空后恢复完整列表」

## 6. 环境变量只读浮层（spec: 环境变量的只读浮层；design D2/D3）

- [x] 6.1 `RequestPreview` 增加 `used`（复用 `ResolvedRequest.used`）并由前端消费；验证方式：`cargo test` 中断言预览结果包含 used 的用例通过，前端单测断言浮层按名称取到值
- [x] 6.2 浮层入口与锚定：环境选择器旁的图标入口（带可供辅助技术读取的名称、键盘可聚焦并触发）、浮层覆盖呈现在选择器下方、不改变任何栏布局；验证方式：dev 环境打开浮层确认其未被会话标签行裁切、侧栏与主区尺寸不变，单测断言入口的可访问名称存在
- [x] 6.3 浮层两段内容：本请求用到的变量（区分已解析/未解析）与当前环境或 Globals 的全部变量；未选中请求时前段以空态说明替代；验证方式：新增单测覆盖「有请求时列出用到与全部两段」「未选中请求时的空态说明」
- [x] 6.4 只读与掩码边界：无就地编辑/新增/删除入口，secret 以掩码呈现且浮层内不存在揭示控件；验证方式：单测断言浮层内没有可编辑输入与揭示按钮，且 secret 值不出现明文
- [x] 6.5 关闭行为与「去环境编辑器」入口；验证方式：单测断言 Esc 与点击外部均关闭且激活环境不变、经入口切换后原请求标签与未保存草稿保留

## 7. 收尾回归

- [x] 7.1 全量自动化回归；验证方式：`npm test`、`npm run test:browser`（复用本机 Chrome）、`cargo test` 全部通过
- [x] 7.2 回归清单（逐条落成自动化断言，`tests-browser/session-bar-and-tables.spec.ts` 与 `tests/app.test.tsx`）——把分隔线拖到两侧极限（地址栏宽度不变、表头仍吸顶）、切到 Environments 再切回 Collections（请求带与草稿恢复）、打开多个标签（标签栏滚动且环境选择器不被挤出）、未选中请求时请求区独占整宽且无分隔线、在 Params 里留一行只有描述的条目后在地址栏敲几个字符（该行仍在、发送时不出现在实际请求里）；验证方式：上述用例通过（纯观感项——配色与间距好不好看——无法由机器判定，留给人工过目）
