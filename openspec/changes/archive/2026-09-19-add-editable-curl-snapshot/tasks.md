## 1. 先验证剪贴板假设（design D5）

- [x] 1.1 在真实引擎里调用一次 `navigator.clipboard.writeText`，确认剪贴板确实拿到内容（以 `tests-browser/session-bar-and-tables.spec.ts` 的「cURL 快照的复制」用例验证并留下回归：授予剪贴板权限后点击复制，再 `readText` 比对改动后的文本）；验证方式：该用例通过。结论：本机 Chrome（Blink，与 WebView2 同族）下可用，无需新增 Tauri 插件；Tauri 窗口里手点一次确认仍建议做（同族引擎结论可沿用，但版本不同）

## 2. 入口与可编辑文本块（spec: 请求带上的 cURL 快照）

- [x] 2.1 在请求带加入「cURL」入口（带可访问名称、`aria-expanded` 反映展开态），点击展开/收起命令文本块；验证方式：单测断言未选中请求时不存在该入口、选中后点击一次即出现文本块、再点一次消失
- [x] 2.2 展开时用当前请求（未保存走 inline 载荷）调用既有 `curlExport`，把命令填入可编辑的多行文本框；验证方式：单测断言文本框的 value 等于桩返回的命令，且元素是可编辑控件（非只读展示）
- [x] 2.3 呈现生成结果给出的不可执行原因；验证方式：单测用带 warnings 的桩返回断言提示可见，且提示里含需要替换的位置
- [x] 2.4 编辑文本块不影响请求；验证方式：单测断言改文本后请求草稿与保存载荷均不变
- [x] 2.5 「重新生成」用当前请求覆盖文本块内容；验证方式：单测断言改文本后点重新生成，内容回到桩返回的命令；且触发过一次新的生成调用
- [x] 2.6 请求在展开期间变化不覆盖文本块；验证方式：单测断言改 URL 后文本块内容不变
- [x] 2.7 「复制」写入的是文本块当前内容；验证方式：单测以桩替换剪贴板 API，断言收到的是改动后的文本
- [x] 2.8 收起再展开即重新生成（编辑不保留）；验证方式：单测断言「改文本 → 收起 → 展开」后内容为按当前请求重新生成的命令
- [x] 2.9 长命令不撑破布局；验证方式：浏览器用例塞入 60 行命令后断言文本块高度不超过 max-height、内部出现滚动、分栏区高度仍大于 150px

## 3. 回归与收尾

- [x] 3.1 全量自动化回归；验证方式：`npm test`（308 通过）、`npm run test:browser`（7 文件 36 条通过，复用本机 Chrome）、`cargo test`（280 通过；1 条失败是与本次无关的既有环境问题——`net::tests.rs` 那条自签 HTTPS 用例需要 PATH 里有 `openssl`）
- [x] 3.2 两处入口口径一致；验证方式：单测断言请求带的入口与「导入 / 导出」模态的入口以同一份输入调用 `curlExport`
