# Tasks

## 1. 超时的两层解析

- [x] 1.1 定义三态超时类型与两张表的解析函数（`跟随全局` / `不限制` / `自定义`，缺省 30 秒），验证：单测覆盖三条分支，并断言「不限制」不产生超时上限
- [x] 1.2 请求设置读库时把老的 `timeout_ms` 升级为三态，验证：单测断言老行 `Some(1500)` 读回为自定义 1500、`None` 读回为跟随全局
- [x] 1.3 应用级超时按既有设置机制读写、缺省 30 秒，0 表示不限制，并在设置非法或负数时回落缺省，验证：单测覆盖未配置、配置生效、0 表示不限制、非法值四条路径
- [x] 1.4 发送路径解析出生效超时并交给客户端组装，只有非「不限制」时才设置超时，验证：对本地慢服务器断言 1 秒自定义超时的请求被中止且结果可区分（改造既有超时用例）
- [x] 1.5 请求级覆盖应用级，验证：单测断言请求级自定义覆盖应用级、请求级「跟随全局」取到应用级设置

## 2. 代理的直连层

- [x] 2.1 拆出「未配置」模式，并把「不使用代理」改为始终生效的一层，验证：单测断言未配置顺位到更低层级、显式直连不被全局代理接管、直连分支可达
- [x] 2.2 按新语义改写既有代理断言，验证：既有代理与白名单用例全部通过，且改动前先看到它们按旧语义失败
- [x] 2.3 请求级代理的模式可选值覆盖四种状态（未配置 / 不使用代理 / 跟随系统 / 手工填写），细节行随之出现与消失，验证：组件测试逐态断言控件与细节行

## 3. 代理凭据的加密落库

- [x] 3.1 落库边界加密代理密码，并把明文登记进日志脱敏出口，验证：单测断言填一个可识别密码后，数据库文件与备份副本中均检索不到该明文
- [x] 3.2 发送路径解密代理密码，凭据不可读取时不阻止请求发出且不携带代理认证，验证：单测断言请求照常发出并呈现代理侧结果
- [x] 3.3 三层读取路径回传掩码视图，能表达「已设置」与「不可读取」，验证：单测断言返回结构不含明文，且两种状态可区分
- [x] 3.4 写入侧区分「不改写凭据」与「清除凭据」，并覆盖两条往返路径，验证：单测断言未提交凭据字段时既有密文被保留、显式清除后密文被删除（误把前者当成后者就是一次静默的凭据丢失）
- [x] 3.5 在安全审计里加明文落库守卫，验证：临时摘掉加密调用后该断言失败

## 4. 请求取消

- [x] 4.1 在发送入参与运行态中引入发送会话，实现按会话撤销在飞请求，验证：单测断言同一会话下的多个在飞请求都被撤销、会话结束后登记项被清除
- [x] 4.2 取消以可区分的结果结束，验证：单测断言取消与超时给出不同的结果类别
- [x] 4.3 响应正文落盘路径纳入守卫，取消时清理未被采用的副本，验证：读正文途中取消后断言响应目录无残留文件
- [x] 4.4 注册取消命令并加入审计白名单，验证：命令面断言通过，且摘掉注册后断言失败
- [x] 4.5 前端脚本段加取消出口，并用既有错误出口回执脚本内挂起的请求，验证：脚本运行中取消后发送流程正常返回、发送态复位
- [x] 4.6 拆出独立的发送态与 `busy`，发送入口按状态切换按钮形态，验证：组件测试断言保存期间不出现取消、发送期间该位置为取消且无法重复发送
- [x] 4.7 取消后的呈现：不动响应区、不弹错误、保留本次已产生的脚本输出，验证：组件测试断言取消后响应区内容不变、无错误提示、脚本输出仍可见
- [x] 4.8 前端侧收尾（会话登记与监听清理）在取消与正常结束两条路径一致，验证：连续取消多次后无累积（用例断言或运行态计数）

## 5. 配置界面

- [x] 5.1 设置模态新增「请求」节三项（超时 / 响应体积上限 / 格式化阈值）：数值与单位同框、0 表示不限制并各有一句提示、只接受非负数，按应用设置机制持久化并立即生效，验证：组件测试断言 0 与负数两条取值口径、改动后往返持久化、且无需重启即影响之后的请求
- [x] 5.2 格式化阈值与体积上限的依赖由控件可选状态表达，验证：组件测试断言超过当前上限的取值不可选，界面上不出现解释该现象的文字
- [x] 5.3 设置模态新增「代理」节承载全局代理，密码控件覆盖「已设置 / 改写 / 清除」三态与「不可读取」状态，验证：组件测试逐态断言，并断言界面上不出现密码明文
- [x] 5.4 环境级代理落在环境自身的编辑面，与全局层可区分，验证：组件测试断言该界面可配置且与全局节不同屏
- [x] 5.5 请求级超时的三态控件与条件数值行，计入既有未保存守卫，验证：组件测试断言三态切换、数值行只在自定义时存在、改动触发未保存标记
- [x] 5.6 修正响应面板里按「阈值大于 0」判断钳制显示的死分支，使前后端对阈值的解释一致，验证：既有响应呈现用例通过，断言不再依赖后端永不产出的取值

## 6. 收口验证

- [x] 6.1 后端测试与前端测试全绿，验证：`cargo test` 与前端测试命令均无失败
- [ ] 6.2 实机冒烟：全局超时填 0（不限制）与填正数、响应体积上限填 0、单请求覆盖、三层代理与「不使用代理」直连、保存代理密码后直接读数据库文件无明文、发送途中取消（含脚本段与大响应下载）
- [x] 6.3 变更本身通过校验，验证：`openspec validate add-network-config-and-cancel --strict` 通过
