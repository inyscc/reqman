## MODIFIED Requirements

### Requirement: 全局细滚动条

应用内的**常驻**滚动容器（侧栏列表、响应正文、表格、模态）SHALL NOT 用占位型滚动条压缩内容宽度：滚动指示 SHALL 以悬浮在内容之上的细条绘制，SHALL NOT 参与所在容器的布局。原生占位型滚动条（出现时吃掉内容宽度、消失时又把宽度还回来）SHALL NOT 出现在这些容器上。

下拉菜单的选项区不在此列：它是一次性浮层（每次打开都是新实例，不存在"同一个容器反复跨过溢出阈值"的横跳），行内也没有必须固定位置的右对齐控件，而原生细条的 thumb 常显、恰是菜单里最可用的滚动暗示——换成"悬停才可见"的悬浮条反而更差。菜单自身滚动该关不关的边界见下方「通用下拉的观感与菜单行为」。

滚动指示 SHALL 仅在容器内容溢出时出现；容器内容不足一屏时 SHALL NOT 出现任何滚动指示。指示出现后 SHALL 在容器被悬停或指示条被拖动时可见，静止时 SHALL 保持克制。指示条 SHALL 可用鼠标拖动，拖动 SHALL 按比例映射为容器的滚动位移。

#### Scenario: 滚动条不明显占宽

- **WHEN** 某区域内容溢出并出现滚动指示
- **THEN** 该区域的内容宽度与内容不溢出时相同，贴右缘的控件位置也不变

#### Scenario: 出现与消失不改变内容宽度

- **WHEN** 同一容器在溢出与不溢出之间切换（改变窗口高度、增删条目）
- **THEN** 行宽与贴右缘控件的位置保持不变，不出现横向跳动

#### Scenario: 内容不足一屏不出现指示

- **WHEN** 容器内容不足以滚动
- **THEN** 不呈现任何滚动指示

#### Scenario: 悬浮指示条可拖动

- **WHEN** 用户按住悬浮指示条并沿滚动方向拖动，随后松手
- **THEN** 容器按拖动位移滚动，指示条随滚动位置同步移动

### Requirement: 通用下拉的观感与菜单行为

界面上从一组互斥选项中取一项的下拉控件 SHALL 由**文本触发器 + 自绘菜单**构成：触发器 SHALL 显示当前选中项的文案并带一个可辨识的展开指示，在静默态 SHALL 呈现为文本而非带边框、带填充的表单控件；展开时的菜单 SHALL 由应用自身绘制并叠加在内容之上，SHALL NOT 使用运行环境自带的原生下拉列表（其字体、行高、选中色与 hover 态均不可控），也 SHALL NOT 撑高或改变它所在行的布局。

菜单 SHALL 满足下列交互与可达性要求：

- 选项 SHALL 可辨识地被悬停 / 聚焦（当前指针或键盘焦点所在项与其周围选项在表面上有区别），当前选中项 SHALL 携带区别于 hover 选中标记的标记；
- 键盘 SHALL 可用：在触发器上按键 SHALL 展开菜单，方向键 SHALL 在选项间移动，Enter SHALL 选中当前项，Esc SHALL 关闭菜单；每个选项 SHALL 具备可供辅助技术分辨的角色与选中状态；
- 选择某选项 SHALL 立即提交该值并关闭菜单；关闭而不选中（点击菜单外部、按 Esc、菜单**之外**的容器发生滚动）SHALL 保持原值不变，SHALL NOT 产生一次"变更"副作用。**菜单自身滚动区域的滚动 SHALL NOT 关闭菜单**——如果关闭，选项超出可视高度就永远无法到达；
- 在触发器上按下指针 SHALL NOT 冒泡为它所在容器自身的拖拽 / 移动语义（会话标签行承担窗口拖拽、请求区承担分栏拖拽），菜单内的操作同理：**菜单的任意位置**（选项、选项之间的间隙、菜单空白处、搜索框、以及菜单自身的滚动指示条）上按下指针 SHALL NOT 触发所在容器的拖拽。菜单以浮层形式渲染（例如挂载到文档根的覆盖层）SHALL NOT 成为漏掉这条约束的理由；
- 菜单 SHALL 适应可用空间：宽度 SHALL NOT 短于触发器，靠近所在容器右边缘时 SHALL 对齐到不溢出的一侧；菜单自身在选项过多时 SHALL 内部滚动而不是把所在行推高。

菜单 SHALL 可按需要提供一个搜索框（见「会话标签行的全局环境选择器」）：搜索 SHALL 按选项文案做大小写不敏感的包含过滤，且 SHALL NOT 改动外部选中值；过滤无命中时 SHALL 呈现明确的空态而不是空白面板。

#### Scenario: 触发器是文本而非表单控件

- **WHEN** 界面渲染一个未展开的下拉
- **THEN** 它呈现为显示当前值的文本与展开指示，不存在输入框式的边框与填充，所在行的高度与该行其它文本成员一致

#### Scenario: 菜单是自绘浮层

- **WHEN** 用户点击触发器展开菜单
- **THEN** 菜单以应用自身的表面绘制、叠加在下方内容之上，不把所在行抬高，也不改变该行其它成员的位置

#### Scenario: 当前项与 hover 项可分辨

- **WHEN** 菜单展开且指针悬停在一个非当前选项上
- **THEN** 当前项与正在悬停项各自呈现可区分的表面，用户能同时看出"现在是哪个"与"将要选哪个"

#### Scenario: 键盘可选择

- **WHEN** 焦点位于触发器，用户按下展开键、用方向键移动焦点后按 Enter
- **THEN** 菜单按 Enter 时焦点所在项提交新值并关闭，焦点回到触发器

#### Scenario: 点击外部不改变值

- **WHEN** 菜单展开后用户点击界面其它位置
- **THEN** 菜单关闭，下拉上的显示与底层取值均保持展开前的值，不产生变更副作用

#### Scenario: Esc 不改变值

- **WHEN** 菜单展开后用户按 Esc
- **THEN** 菜单关闭、原值不变

#### Scenario: 滚动关闭

- **WHEN** 菜单展开后菜单**之外**的容器发生滚动
- **THEN** 菜单关闭且选择结果不变

#### Scenario: 菜单自身滚动不关闭菜单

- **WHEN** 菜单展开后用户在菜单内部滚动（滚轮，或拖动菜单自身的滚动指示条）
- **THEN** 菜单保持展开，选项按滚动位移移动，全部选项可达

#### Scenario: 不触发所在容器的拖拽

- **WHEN** 用户在会话标签行的下拉上按下指针并移动
- **THEN** 下拉不参与窗口拖拽，也不改变标签行本身的选中项

#### Scenario: 菜单内任意位置都不触发容器拖拽

- **WHEN** 用户在展开的菜单内部按下指针并移动——包括选项之间的间隙、菜单空白处与菜单自身的滚动指示条
- **THEN** 菜单不参与窗口拖拽（窗口不移动、不触发最大化），也不参与请求区的分栏拖拽

#### Scenario: 右边缘处不溢出

- **WHEN** 靠近容器右边缘的下拉被展开
- **THEN** 菜单对齐到容器内侧，不产生横向溢出，也不被裁剪

#### Scenario: 选项过多时菜单内部滚动

- **WHEN** 展开一个选项数量超过菜单可视高度的菜单
- **THEN** 菜单在自身范围内滚动，所列选项全部可达，所在行与周围布局不受影响

#### Scenario: 搜索过滤选项

- **WHEN** 带有搜索框的菜单被展开，用户在搜索框内输入一段文本
- **THEN** 列表只显示文案包含该文本的选项（大小写不敏感），尚未选中时外部的值保持不变

#### Scenario: 搜索无命中显示空态

- **WHEN** 用户输入的文本不匹配任何选项
- **THEN** 菜单区域内显示明确的空态提示，而不是一片空白面板
